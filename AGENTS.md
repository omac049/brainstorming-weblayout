## Learned User Preferences

- For **broad site and organic research**, use roughly **12 months** of **GSC / GA4** data; for **paid landing pages** and **Layout Score Builder** inputs, prefer **~30-day** windows when recent traffic allocation matters (12-month ranges can skew inactive paid URLs). **Lead-funnel** claims should align with Kevin's program report (**Oct 2025 – Mar 2026** primary BigQuery window). Lean on **GSC, GA4, BigQuery**, and other connected tools to keep numbers defensible.
- For deck or report claims about the **live site** (e.g. how many RFIs, where forms sit) **and Phase 2 clones**, **verify on the real pages** / captures instead of inferring from older copy, aggregate labels, or assumed brand patterns. The **RFI** is the request-information **form**; it can appear on **many** pages, so avoid implying a **single** “primary RFI page” unless analytics or copy explicitly scope to one URL.
- When comparing or listing **organic pages to review**, treat **`online-degrees/` (program) routes** and **blog posts** as separate buckets because intent differs. **Organic prototypes** (`~/uagc-prototypes` **`/organic/…`**) target **layout/UX sign-off**, not pixel clones—use **`.cursor/skills/clone/SKILL.md`** + **`design-system/clones/*.json`** (homepage: **`homepage-clone.json`**) for live nav, hero images, and copy; organic heroes follow **paid hero visual style without in-hero RFI** (live **`hero.webp`**, paid-hero height—faces visible, not cropped); organic **homepage** (`/organic/homepage`) uses an **empathy-first** arc (visitor situation before institutional pitch)—hero **“Finish Your Degree on Your Schedule”** (not paid **“Earn Your Degree 100% Online”**), **`$485/credit`** gold-accent trust pill + WSCUC; **`ValuePropsSection`** (**VP-01**) with **visitor-benefit** cards (“Your credits count here,” not policy labels); **`TestimonialSection`** (**TRUST-01**) **early** (~position 3)—**3-card grid** with **persona tags** and short scannable quotes; **`HomeDifferentiatorPathSection`** (**Find Your Path** — subhead **“Degrees, admission, and financial aid — explore what fits your goals.”**; interactive **Online Degrees / Admission / Financial Aid** tabs with bordered on-page cards—improve the prototype pattern, not a static live clone; + **Are You Ready to Start?** / **CTA-01** only—**drop “How UAGC Is Different”**; it duplicates **VP-01** / **Why Students Choose UAGC**; make **Find Your Path** meaningfully interactive—replaces separate **`HomeDifferentiatorSection`** + bottom **`HomeMultiPathCTA`**); when fixing **scroll/card fatigue**, **keep module order** and refactor **within-component layouts** only—catalog IDs, Figma, and Drupal depend on stable positions (user rejected reordering, e.g. moving **TRUST-01**); prefer **flat rows, dividers, and non-table comparison patterns** over back-to-back card grids—**UAGC vs traditional** comparisons should **not** default to tables; **`ProgramExplorer`** (**PROG-01**) for program discovery—**not** **`HomeInterestGrid`** + **`HomeProgramPreview`** (redundant); **`HomeNewsSection`** → **`AccreditationBand`** (UA horizontal logo ~**`h-10`/`h-12`**, not a tiny square); **`WaysToSaveSection`** (**SAVE-01**-style on-page expandable cards); closing **`FORM-02`** then **`FAQ-01`** (objection handler just above footer) + sticky bar (**no mid-page RFI**); **`HomeDifferentiatorPathSection`** closes with **`CTA-01`** four-card grid (chat / call / request info / apply)—not a simple **“Ready to Start?”** strip; organic hero: **no in-hero “Find Your Program”** CTA—trust pills only (**`$485/credit`**, **5-Week Courses**, **WSCUC Accredited**, **`$0 to Apply`**) with extra spacing below subhead; **`WaysToSaveSection`**: **`bg-white`** with **`#faf9f7`** cards/trust banner/CTA so it separates from **`AccreditationBand`** (**`#faf9f7`**); use **multi-open** stat-forward disclosure cards (86% lead anchor); adjacent homepage sections need **distinct background colors**—don't let near-identical tints (**`#faf9f7`** vs **`#fafafa`**) run together; **same catalog components as paid, different module order**; section-nav anchors must match real section IDs; **`HomeNewsSection`** = evergreen “Recognition & Achievements,” not dated articles; **WSCUC / accreditation copy:** do **not** name peer universities (Stanford, UCLA, USC, etc.)—convey prestige via WSCUC standards language only; **utility nav** keeps live **Military** / **Partnerships** dropdowns; **Apply Now** (red) pairs with **Request Info** in the **main header**, not the utility bar—use **GA4/GSC** (~12 mo) to prioritize nav labels; section-nav **Find Your Path** anchor = **`#degree-path`** (not **`#differentiators`**); organic messaging reviews use **`simulation_homepage.csv`** and **`simulation_online-degrees.csv`**—favor **program-breadth specificity**, **concrete cost examples**, and **outcome proof** over generic flexibility copy; **online-degrees hub** (`/organic/online-degrees`) is **discovery-first**—**no hero or mid-page RFI** (header **Request Info** + sticky bar only); reuse shared prop-driven **`OrganicHomeHero`** + organic **`SiteFooter`**; **`HomeDifferentiatorPathSection`** splits via **`show`** (**`tabs`** right after hero, **`cta`** below **`PROG-01`**); **`TRUST-01`** early after browse sections; **`PROG-01`** replaces live degree-quiz blocks.
- For **affiliate / vendor compliance** audits, crawl **Vendor / Page_URL** from the team sheet ([Google Sheet](https://docs.google.com/spreadsheets/d/1D85xfq5KyoNks3Iewcl16bZmVC4IMZVkxrMh1ARoxB4)) in **~5-URL batches**—capture **UAGC-specific marketing copy** (what partners say about UAGC), **logo URL**, and **full-page screenshots** per page (not just form fields); logos may be CDN-hosted on partner flows (e.g. Zeta **`uagc_zeta`** / **`uagc_zeta_logo.png`** on CloudFront).
- For **benchmark or “others solved this”** content, **cite sources** for the figures shown.
- **LFS / LSF Leads** (wording varies in UI and docs) are **deduplicated** program leads; counts often **differ from GA4** and are not interchangeable with “all conversions” without matching definitions and filters.
- For **Google Ads** (notably **PMax**), the default **Conversions** total includes **only** conversion actions marked for that metric and may track **deeper-funnel** events—not necessarily form-fill or deduped lead volume. For **lead counts** and **cost per lead**, use **per–conversion-action** data (e.g. **LFS / LSF Leads**) or agreed CRM/lead definitions; do not treat headline **conversions** ÷ **spend** as CPL without confirming which actions are in the column. **Legacy Google Discovery** CPL figures are a weak benchmark—often **spam-heavy** and **de-prioritized or shut down**; do not stack them next to Search/PMax leads without that context.
- Phase 2 paid landing work targets a **“10x improve” layout/UX redesign** reusing the **26-module catalog** and **Project Groundwork** audit data—not pixel-perfect clones; fold in **competitor patterns** and connected analytics. Treat **layout/conversion paths** as the current phase and **messaging/copy refinement** as follow-on.
- For **paid landing prototypes** and **A vs B** pages, frame the story as **content and layout** around **shared or similar form patterns**; prioritize **mobile-native** UX (most traffic), a **digestible hero** (~**75/25** split, **two-step RFI** like **`/success/request-info-v5`**), and **hero imagery** that stays visible (RFI shouldn’t cover faces; headline overlays shouldn’t obscure the photo on mobile; avoid dead **white space** above the RFI—keep subhead/support copy in the hero overlay when needed). **Mobile sticky bottom RFI bar**: show **only after scroll**, not on initial hero view. **`request-info-v5`** hero headline: **“Earn Your Degree 100% Online at UAGC”**. **`online-college-courses-v5`** is intentionally **lean (~10 substantive modules, ~8–9 iPhone screens)** — not the full v5/v7 stack; proof via OCC-exclusive **`SKEPT-01`** / **`SkepticismBusterSection`** instead of **`ProgramExplorer`** / career/salary/credential modules. Use **`simulation_online-college-courses-v5.csv`** for OCC messaging (program breadth, cost transparency, teacher-licensure and military-benefits FAQ are high-switching-risk themes). **De-prioritize “3-Week Risk-Free Trial”** as a lead value prop on v5/v7; OCC may foreground trial/format hooks per its lean spec. **`FIN-01` / `TuitionSection` “Ways to Save”**: keep users **on-page**—use **expandable disclosure cards** with digestible bullets distilled from live fin-aid URLs (**no outbound links** off the landing). **UA affiliation / reputation copy** must stay **defensible**—avoid overreaching superlatives (e.g. **“top 100 universities worldwide”**); prefer grounded **enterprise / R1 / WSCUC** language. **After hero on all three paid templates:** **`START-01`** → **`TRUST-02`** banner → **`VP-01`** (Reasons to Choose UAGC)—**not** VP-01 directly under hero; refresh **`UpcomingStartDates`** **`DEFAULT_DATES`** in **`~/uagc-prototypes`** when terms pass or the urgency badge goes stale—**Generous Transfer Policy** as the **first highlight card**; keep card layout and bullet content **consistent across the three target pages** (unified stat/non-stat card structure; bullets share card icon treatment); **`ValuePropsSection`** = highlight cards + experience callout only—**no `bulletPoints`** that repeat **`TrustStrip`** trust signals; **no section-level RFI CTA** (redundant near **`ProgramExplorer`**, mid-page RFI, sticky bar, **`UpcomingStartDates`**). Use **flat colors only** (no gradients, alpha overlays, backdrop-blur, decorative shadows unless requested). **Start dates** belong in a dedicated **`UpcomingStartDates`** module (quiet urgency—next two dates, informational only); **`TrustStrip`** third badge = **barrier-removal value prop**, not enrollment dates or **“Enroll now”**. Don’t **duplicate career/trust story** between **`CareerOutcomesSection`** and **`EmployerCredentialSection`** (e.g. Handshake / lifetime services once)—also **don’t stack `ProgramComparisonSection` next to `CareerOutcomesSection`**; use **one consolidated career-outcomes beat** (comparison table + Handshake callout). **`BenefitsSection` / tuition-aid**: present **bite-sized chunks**; on split layouts, **align section headers and card rows** across columns. On mobile prototype reviews, enforce **accessibility-minded touch targets and readable type** (e.g. hero highlight pills not sub-12px; **~44×44px** minimum on header/footer/nav controls). For **true mobile viewport** checks, use **Playwright** (Chromium + WebKit)—**Cursor IDE browser resize** often does **not** trigger CSS media queries. **`SalaryGrowthSection`**: **link cited sources inline** (e.g. **BLS** earnings data). Bottom **“Ready to Start Your Degree?”** should offer **multiple paths**: chat with advisor, call, request information, apply now. On **`uagc-navy`** / dark sections, keep **contrast** solid—avoid faint **`text-white/50`–`/70`**; typography utilities like **`.type-h*`** should **not** hardcode color (set **`text-white`** / **`text-uagc-navy`** on the component). Large **cross-URL** conversion gaps often reflect **traffic source and intent**, not layout alone—tie narratives to **channel mix** and tests that **held traffic constant**. Reuse **`TrustStrip`** below **“It only takes a minute. No obligation.”** — **`sidebar`** stack in narrow hero panels (~380–460px); **`banner`** row below the hero or in full-width bands. **`ProgramExplorer`** / program discovery: keep users **on-page** with searchable in-page catalog without lengthening or heavying the page; **no per-program “Request Info” CTA**—use **expandable rows** with rich on-page program detail so students don't leave to learn basics; copy **50+ programs**, not **200+**—transfer policy belongs in **`ValuePropsSection`**, not a **`ProgramExplorer`** transfer callout (default off). On mobile, **avoid nested scroll traps** (`max-h` + `overflow-y-auto` program lists, horizontal filter-pill strips)—show **~6 programs** in normal page flow with **Show All** expand; use a **native `<select>`** for area filters; desktop may keep scrollable list. Target **~12–13 iPhone screens** total prototype height (`degree-programs-v7` benchmark; live v5 ~10). **`TRUST-01`** / **`TestimonialSection`**: **3-card responsive grid** (not carousel or single wall-of-text quote)—**persona tags** (Working Parent, Career Changer, Military Veteran, etc.), short scannable quotes, initials + verified badge, optional **subheading**; paid templates heading **“Students Like You Are Already Here”**; when modernizing, sync **prototype**, **`design-system`** specs/manifests, **Figma** wireframes + componentry + **source capture** pages, **`layout-builder.html`**, and **Drupal handoff**. **Paid-landing prototype colors:** keep **gold** accents (`#EF9600` / **`uagc-gold`**) for CTAs, fee-waiver buttons, **START-01** calendar, **FIN-01** disclosure checkmarks, and **EMOT-01** highlights—**not** a strict Reskin navy/sky/red CTA split; a Reskin rebrand pass was rolled back (**keep Fira**). Module backgrounds: **`TuitionSection`** `#faf9f7`, **`EmployerCredentialSection`** `#fdf8ef`.
- For **ELT** slides and **re-skin / roadmap** copy, keep **Google Ads / ad-budget** material **light** when the speaker does not own that area, but **surface data gaps and metric definitions** where leadership needs accuracy; use **grounded, accurate** phrasing for what improved visually vs. what is **next** for layout, structure, and paths—not claims that misfit program status.
- For **`index-v2.html`**, use **arrow-key navigation** for advancing slides (**do not** add **click-to-advance** on the slide surface if it makes the deck hard to use); keep **`presenter.html` talking points** aligned with **v2 text and order**, with **comparative, plain-spoken** punchlines (wit helps when it fits the room).
- For **screenshot-based audits and handoff**, prefer the **HTML gallery** under **`output/playwright/screenshots/`** (`index.html` and related task pages) over **Figma** when **every capture** must stay visible and navigable; document **sticky / persistent RFI** where it appears on pages; for **paid vs organic** comparisons, use **real paid landing URLs** (typically **`/success/…`**) vs **organic** destinations, not only **template version** pairs (e.g. v7 vs v5). Long audit pages (e.g. **`1-1-screenshots.html`**) should use **collapsible anchor rails** (progress + expand on hover)—helpful but **not** wide fixed sidebars that compete with content; **active nav pills** should **adapt contrast** when the rail overlays **same-color (navy) section backgrounds** so they don't blend in. **Contentsquare** heatmap / attention / scroll exports for **`1-5-contentsquare.html`** live under **`content_square/`**; when committing large image sets to Git, **compress or resample** (e.g. JPEG with width caps) rather than defaulting to full-size PNGs. For **GitHub-hosted** static pages in this repo, keep **core content visible by default**—do not rely on **`opacity:0`** plus **JavaScript** (e.g. **IntersectionObserver** “reveal” patterns) as the **only** path to readability; animations should be **progressive enhancement**, not a gate for base visibility.
- Before **GitHub pushes** that touch APIs, env vars, or new scripts, run a **security scan** (secrets / dependency checks) and fix issues first.

## Learned Workspace Facts

- **Remote:** `git@github.com:omac049/brainstorming-weblayout.git`
- **Project name:** **Project Groundwork**. **Long-form deck:** `index.html` (~20 slides, source for extra pulls); **ELT cut:** `index-v2.html` (exec version); **presenter notes:** `presenter.html` (keyboard-synced, aligned to v2).
- **No in-repo Docker:** static HTML decks plus `data/` assets; there is **no** `Dockerfile` or `docker-compose` here—container work would be **additive**, not a fix to existing config.
- **Stakeholder shorthand:** **Kevin** — VP, Performance Marketing (paid, PMax, CPL and **conversion-action** definitions); his **program report** ([kevinseaman-krs.github.io/uagc-program-report/](https://kevinseaman-krs.github.io/uagc-program-report/)) uses BigQuery **`advertising-data-mart.inquiries`** with **Oct 2025 – Mar 2026** primary lead-funnel window — align funnel comparisons to that. **Christie** — Arizona Online (site experience, RFI/UX questions). Useful when attributing feedback or data ownership.
- **Paid search vs site:** full **University of Arizona Global Campus**-style terms often drive **more query volume** than **UAGC**-style shorthand in **paid search**; paid strategy may **emphasize the full name**—**site** navigational nickname use is **not** the same lens as **ads**.
- **Paid numbers** in `README.md`, `data/paid-source-data.md`, `data/claims-checklist.csv`, and related **`data/paid-*.csv`**: **headline “conversions”** in Ads-style tables and API pulls are **action-scoped**; **lead** claims need the matching **conversion action** or lead-system field—not the aggregate conversions column by default. After edits, keep **README**, markdown sources, **CSV** extracts, and **deck** copy **aligned**.
- **Live-site capture library:** `output/playwright/screenshot-pages.js` reruns **full-page** Playwright captures for the agreed **`https://www.uagc.edu`** page set (**`/success/…`** for paid landings); PNGs land in **`output/playwright/screenshots/`** named **`{page-slug}_desktop-1440.png`** and **`{page-slug}_mobile-375.png`**, with **`README.md`** (metrics summary) and **`index.html`** (desktop/mobile preview + tables). Broader reviews may separate **top paid** vs **top organic** URL sets (e.g. **10 + 10**); Phase **~1.1–1.5** audit docs often reuse the **same captures**, so **`index.html`** plus optional **per-task HTML** in that folder anchors the checklist work; audit pages may add **component/module labels** and note **repeated modules** across URLs. **Layout Score Builder** (`layout-builder.html`) scores layouts **mobile-first** against the **26-module UI catalog** and audit data. **Task 1.10** thank-you audit uses base **`/request-information/thank-you`** (not personalized portal variant). **Contentsquare** captures for **`1-5-contentsquare.html`** sit under **`content_square/`** in the same folder — **ingest, filenames, and Visual Evidence grid** are in the **`content_square/`** bullet at the end of this section.
- **Phase 2 prototypes:** sibling Next.js repo **`~/uagc-prototypes`** (from **ai-website-cloner-template**) is an **interactive spec/reference**—**Drupal** (paragraphs + theme) is the production target; **`design-system/DRUPAL-HANDOFF.md`** maps catalog IDs → paragraph types, fields, and JS for the three paid templates. **Paid** routes: **`/success/request-info-v5`**, **`/success/degree-programs-v7`**, **`/success/online-college-courses-v5`** (not **`/military-v5`**). **Organic** routes under **`/organic/…`**: **`/organic/homepage`**, **`/organic/online-degrees`** hub, program pages (e.g. business administration), and thank-you—specs in **`design-system/pages/online-degrees-hub.md`**, **`design-system/pages/request-information-thank-you.md`**, and matching **`design-system/componentry/*-modules.json`**; homepage organic-only components in **`src/components/organic/`** (`OrganicHomeHero`, `HomeDifferentiatorPathSection`, `WaysToSaveSection`, `AccreditationBand`, `HomeNewsSection`, **`SiteFooter`**; legacy **`HomeInterestGrid`** / **`HomeProgramPreview`** / **`HomeDifferentiatorSection`** / **`HomeMultiPathCTA`** superseded on homepage by **`PROG-01`** + **`HomeDifferentiatorPathSection`** (**Find Your Path** + **`CTA-01`** only—no separate **How UAGC Is Different** grid)); organic **`SiteFooter`** clones live **`uagc.edu`** structure (compact: contact band + flat degree-area row + legal baseline)—**not** paid **`Footer`**; **no UAGC / Arizona logos** in footer (text affiliation only); drop **Information For** / heavy degree sub-level grids; **online-degrees hub** is **navigational/discovery-first**—**no hero or mid-page RFI** (header **Request Info** + **`FORM-05`** sticky only); stack **`HERO-ORG`** (shared **`OrganicHomeHero`**) → **`HomeDifferentiatorPathSection`** **`show="tabs"`** → **Areas of Study** → **`TRUST-01`** → tuition band → **`PROG-01`** → **`show="cta"`** → **`FAQ-01`** → **`SiteFooter`**; clone **`design-system/clones/online-degrees-clone.json`**. Organic homepage closing stack: **`SAVE-01`** → **`FORM-02`** → **`FAQ-01`** → **`SiteFooter`** → sticky bar. **`~/uagc-prototypes`** dev server often binds **port 3002** when **port 3000** is occupied (e.g. Docker). Goal is **modular “10x improve” redesign**, not pixel cloning. **`request-info-v5`** and **`degree-programs-v7`** share the full ~16-module stack (reuse **`TrustStrip`**, **`RFIForm`**, **`ValuePropsSection`**, **`ProgramExplorer`**, **`UpcomingStartDates`**, **`CareerOutcomesSection`**, **`EmployerCredentialSection`**, **`SalaryGrowthSection`**, **`FAQSection`**, **`EmotionalMotivationSection`**, etc.)—on **`request-info-v5`**, **`ProgramComparisonSection` is consolidated into `CareerOutcomesSection`** (“Career Outcomes by Program” table + Handshake callout), not a separate adjacent section; **`degree-programs-v7`** uses **full `ProgramExplorer`**. **`online-college-courses-v5`** is **lean (~10 substantive modules, ~8–9 iPhone screens)** with shared chrome only (**`NAV-01`**, **`NAV-UX-01`**, **`START-01`**, **`TRUST-02`**) plus OCC-exclusive **`SKEPT-01`** (**`SkepticismBusterSection`**)—no **`ProgramExplorer`**, **`CareerOutcomesSection`**, **`SalaryGrowthSection`**, **`EmployerCredentialSection`**, or **`EmotionalMotivationSection`**. **`online-college-courses-v5`** hero image should match the live paid page (**`UAGC_WEB_Landing-Page_Hero-Images_v1_Page_9.jpg`**, served from **`uagc.edu`**—already allowed in **`next.config.ts`**). **Persona simulation CSVs** (repo root) feed messaging reviews for matching prototypes—paid: **`simulation_request-info-v5.csv`**, **`simulation_degree-programs-v7.csv`**, **`simulation_online-college-courses-v5.csv`**; organic: **`simulation_homepage.csv`**, **`simulation_online-degrees.csv`**. When v5/v7 wireframes and componentry already exist, scope OCC-only spec/Figma/prototype work unless explicitly asked to reshape v5/v7. **Paid landing module checklist** (career-advancement persona): **base** = hero+RFI, start dates, tuition, testimonial, career outcomes (job titles + salary), trust, FAQ, Tier-3 emotional module; **audience-specific** = salary growth viz, employer credential callout, prior-learning callout. Live **“The Benefits of a Global Campus Degree”** maps to **`CareerOutcomesSection`** (**“Career Support and Graduate Outcomes”** / consolidated outcomes table). **Lightcast** career-outcomes widget embed codes live in **`EmbedCodes-LightcastWidget(Sheet1).csv`**. Use **crawl4ai** / live captures for CSS fidelity when matching live Drupal styling (**Fira Sans**, tokens). **Scrapling MCP** in global **`~/.cursor/mcp.json`** (`uvx --from scrapling[ai] scrapling mcp`) complements crawl4ai for fetch, stealth scrape, and screenshots. **Google Sheets MCP** (`google-sheets` via **`uvx mcp-google-sheets@latest`**; **`DRIVE_FOLDER_ID`** `1D6EfFVfdMhzqEnykpACxvU9Fdw5YKX7g`; **`uagc-seo-monitor@advertising-data-mart.iam.gserviceaccount.com`**) for agent spreadsheet read/write. Landing briefs: **`uagc_landing_page_agent_brief.md`** (persona simulation, proof/skepticism themes) and [uagc-audience-landing-page-strategy](https://brandyblack13.github.io/uagc-audience-landing-page-strategy/). **UI/UX skill:** **`.cursor/skills/ui-ux-pro-max/`** (`search.py --design-system` for reviews). **`UpcomingStartDates`**: compute urgency (`daysLeft`) at **render** time—avoid `useEffect`-only badge state (SSR/hydration mismatch on “Starts tomorrow”).
- **Component manager skill:** **`.cursor/skills/uagc-component-manager/`** (`scripts/registry.py`) tracks **35 modules** across paid/organic/hub/thank-you templates—use **`list`**, **`show`**, **`page`**, **`shared`**, **`exclusive`**, **`diff`**, and **`drupal`** for reuse audits and Drupal paragraph mappings.
- **Design system:** **`design-system/MASTER.md`** is the canonical in-repo token reference (historical extract from external Reskin file `c4O4uPeilJDBZFjARNnt65` — **no team Figma/MCP access** to that file); **`design-system/RESKIN-ALIGNMENT.md`** tracks typography/token freshness vs the editable wireframes file (**24 local text styles**: 12 Desktop + 12 Mobile in **`HoSMZOSnKSVgUXlskHv9tS`**). Do **not** conclude Figma pages are empty from a partial MCP/metadata read—**`setCurrentPageAsync`** each page before auditing. It documents **color palette** (navy `#0C234B`, red `#AB0520`, crimson `#C10121`, neutrals), **typography scale** (Proxima Nova Extra Condensed / Proxima Nova for Figma; **Fira Sans** as the live-site web font), **heading + paragraph scales** (desktop and mobile), **link states**, **icon inventory** (Figma component keys), **CSS custom properties**, and **Tailwind config** extensions. All prototype and audit work should reference this file for token values rather than eyeballing colors or guessing font sizes. Page-specific overrides go in **`design-system/pages/<page>.md`** — for **`request-info-v5`**, use **`design-system/pages/request-info-v5.md`** (dev handoff), **`design-system/componentry/request-info-v5-figma-build-guide.md`** (Figma componentry page spec), **`design-system/componentry/request-info-v5-modules.json`** (machine-readable module manifest); for **`degree-programs-v7`**, use **`design-system/pages/degree-programs-v7.md`**, **`design-system/componentry/degree-programs-v7-figma-build-guide.md`**, and **`design-system/componentry/degree-programs-v7-modules.json`**; for **`online-college-courses-v5`**, use **`design-system/pages/online-college-courses-v5.md`** and **`design-system/componentry/online-college-courses-v5-modules.json`** (lean stack + **`SKEPT-01`**). Use skill **`.cursor/skills/figma-landing-componentry/SKILL.md`** when building or rebuilding Figma componentry (desktop 1440 + mobile 375 per module). **Figma wireframes + componentry file:** [`HoSMZOSnKSVgUXlskHv9tS`](https://www.figma.com/design/HoSMZOSnKSVgUXlskHv9tS/UAGC-Paid-page-templates-3--%E2%80%94-Wireframe?node-id=0-1) in [team project 598288008](https://www.figma.com/files/team/954051891409200881/project/598288008?fuid=1052358699765663783) (`design-system/FIGMA-FILES.md`) has **12 populated pages** (v5/v7/OCC wireframes, Desktop 1440, Mobile 375, Componentry per template) — **External Reskin file (`c4O4uPeilJDBZFjARNnt65`) is not accessible** — all Figma writes and MCP audits use the wireframes file only (`HoSMZOSnKSVgUXlskHv9tS`). When auditing via **`use_figma`**, call **`setCurrentPageAsync`** before reading page **`children`** — incremental loading can report **`childCount: 0`** on unvisited pages. **HTML captures import at ~1853px** — normalize desktop wireframes to **1440**; desktop module catalog previews must be **1440px** wide (not undersized ~1392 clones). **Componentry = Components** (not Assets); module cards use **full-height** wireframe clones—**no 1100×480 height clips**. **`request-info-v5 - Componentry`** is the **canonical layout reference** for rebuilding **`degree-programs-v7 - Componentry`** and OCC—match v4 cards, spec sidebars, library grids, and asset sizing (not OCC as the v7 template). **`online-college-courses-v5 - Componentry`** remains the most complete for promoted **`Landing /`** Components incl. **`SKEPT-01`**. File still lacks Figma **design-token variables**; only OCC has promoted Components in Assets. **`03 - Landing Components Library`** and mobile grid cards are **468×HUG** with **448px-wide** inner previews—clone **full** catalog-preview frame height; never clip exports (e.g. **`HERO-01`**). **`03`** library previews must stay **editable**—clone catalog wireframes and **`rescale()`** to **448px** / **200px** (not PNG/SVG imports, which flatten text). Create **`online-college-courses-v5-figma-build-guide.md`** when OCC Figma work stabilizes. Prototype impact headlines use **Fira Sans Extra Condensed** (800) as the web stand-in for Figma Proxima Nova Extra Condensed. When prototype **UX or messaging/copy** changes, re-capture wireframe pages and update Componentry (**`01 - Desktop Module Catalog`**, **`02 - Mobile Module Catalog`**, **`03 - Landing Components Library`** with mobile variants) so Figma stays in parity with code. **`generate_figma_design`** often leaves **`NAV-01`** logos as **empty image placeholders**—repair with text nodes or **`upload_assets`**, then refresh **`03`** library clones. **`03`** component **container frames** must wrap **full component bounds**—no undersized clip boxes (e.g. **`VP-01`**, **`EMOT-01`**); **`NAV-UX-01`** pills and **`NAV-01`** header logo/phone must match prototype **`SectionNav`** / header exactly.
- **`/apply-now`** redirects to the **Salesforce** application intake on **`cloud.mail.uagc.edu`** — captures and “what’s on the page” discussions should treat it as **off-domain**, often **minimal UI** versus marketing pages.
- **`content_square/`** (Contentsquare exports for **`1-5-contentsquare.html`**): repeatable ingest is **PNG** drop → **`sips`** to **JPEG** (typical **quality ~55**, max width **~1200px** desktop / **~600px** mobile) → update image **`src`s** in **`1-5-contentsquare.html`** → drop **PNG**s from the tracked set (or **`.gitignore`** if keeping local full-res masters). Use **hyphenated CamelCase** filenames with **no spaces** (e.g. **`Homepage-Desktop-Heatmap.jpg`**, **`OnlineDegrees-Mobile-Scroll.jpg`**) so relative URLs work on **GitHub Pages**. **Visual Evidence** sections use a **3-column** layout (**`grid-template-columns: repeat(3, 1fr)`**) for **desktop heatmap / mobile heatmap / scroll** when all three assets exist for a page.
